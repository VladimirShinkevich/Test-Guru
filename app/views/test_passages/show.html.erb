<h3> Passing the <%= @test_passage.test.title %> test </h3>

<b> Question: <%= @test_passage.number_of_question %>) <%= @test_passage.current_question.body %> </b> <br>
<b> Question count: <%= @test_passage.test.questions.size %> </b>

<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id, :body, include_hidden: false do |b| %>
    <p> 
      <%= b.check_box id: "answers_ids_#{b.object.id}" %>
      <%= b.label %>
    </p>
    <% end %>
    
    <p> <%= form.submit 'Далее' %> </p>

<% end%>
