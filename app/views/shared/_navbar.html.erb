<% if logged_in? %>
  <p class="nav user"> <b> <%= current_user.name %> welcome to Guru </b> <br>
    <%= link_to 'Exit', logout_path, method: :delete %>
  </p>
<% else %>
  <p class=" nav user">
    <%= link_to 'Login', login_path %> |
    <%= link_to 'Registration', singup_path %>
  </p>
<% end %>
