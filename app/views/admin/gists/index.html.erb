<div class="card justify-content-center mt-3 border-primary">
  <div class="card-body">
    <h1 class="text-center"> <%= t('.gist') %> </h1>

    <table class="table table-bordered table-hover table-success table-striped text-center">
      <tr>
        <th> <%= t('.user') %> </th>
        <th> <%= t('.question') %> </th>
        <th> <%= t(".url") %> </th>
        <th> <%= t('.action') %> </th>
      </tr>

      <%= render @gists %>

    </table>
    <%= link_to t('.back'), admin_tests_path, class: 'btn btn-primary' %>
  </div>
</div>
